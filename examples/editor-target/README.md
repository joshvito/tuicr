# editor-target fixture

Fixture for manually testing `e` (open in $EDITOR) during PR review.
The head branch of the fixture PR modifies, adds, and deletes files here so
each editor-target case can be exercised in one review. Not used by any build.
